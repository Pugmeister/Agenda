<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <title>Post Maker Bot — Сохранить пост</title>
  <link href="https://rsms.me/inter/inter.css" rel="stylesheet" />
  <link rel="stylesheet" href="/static/css/style.css" />
</head>
<body>
  <div class="container">
    <div class="card">
      <div class="flex-between mb-1">
        <h2>Создать пост</h2>
        <button type="button" class="btn btn-secondary" onclick="location.href='/'">← К отправке постов</button>
      </div>
      <form action="/create" method="post" enctype="multipart/form-data">
        <div class="form-group">
          <label>Название проекта</label>
          <input name="project" type="text" required placeholder="например, kastrulivideo"/>
        </div>
        <div class="form-group">
          <label>Медиа-файл</label>
          <input name="media" type="file" accept="video/*,image/*" required/>
        </div>
        <div class="form-group">
          <label>Текст поста (HTML)</label>
          <textarea name="caption" rows="6" placeholder="Текст HTML…"></textarea>
        </div>
        <button type="submit" class="btn">Сохранить</button>
      </form>
    </div>
  </div>
</body>
</html>